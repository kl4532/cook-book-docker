build:
  docker:
    database: database/Dockerfile
    backend: cook-book-api/Dockerfile
    frontend: cook-book/Dockerfile
release:
  image: frontend
  command:
    - echo 'Starting frontend after deployment!';
